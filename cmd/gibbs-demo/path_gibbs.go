//go:build gibbs

package main

const keygenPath = "Gibbs sampler (Sun et al., PQCrypto 2026; alpha=1.04, epsilon=0.01)"
const keygenWarning = "Keys are research-only — NOT interoperable with mainnet FALCON verifiers."
